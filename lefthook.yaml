# Lefthook configuration for blog.alswl.com
# Simple configuration that delegates to hack/format.sh

pre-commit:
  commands:
    format:
      run: bash hack/format.sh {staged_files}

pre-push:
  commands:
    format-check:
      run: bash hack/format.sh